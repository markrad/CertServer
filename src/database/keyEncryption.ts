/**
 * Enum representing the encryption options for keys.
 */
export enum KeyEncryption {
    /** No encryption */
    NONE = 0,
    /** User encrypted */
    USER = 1,
    /** System encrypted */
    SYSTEM = 2,
};

